#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to format and lint staged files
npx lint-staged

# Run type checking on the entire project
npm run type-check

# Run tests with watch mode disabled for CI-like behavior
npm run test --watchAll=false
